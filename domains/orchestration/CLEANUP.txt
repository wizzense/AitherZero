# Orchestration Cleanup Plan

## KEEP (1 playbook)
- playbooks/testing/test-orchestrated.json

## DELETE (56 playbooks - 98% cleanup)
Everything else - they're all examples, experiments, or duplicates

## New Structure
orchestration/
  ├── playbooks/
  │   └── test-orchestrated.json  (THE ONLY ONE)
  └── schema/
      └── playbook-schema-v3.json (keep latest schema only)
