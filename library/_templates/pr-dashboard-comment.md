## 📊 Dashboard & Container Published

Your PR-specific dashboard and Docker container have been published!

### 🔗 Quick Links
- **[View PR Dashboard]({{PAGE_URL}}library/reports/pr-{{PR_NUMBER}}/)** - Your PR's current status
- **[Main Project Dashboard]({{PAGE_URL}}library/reports/dashboard.html)** - Overall project health
- **[All Test Results]({{PAGE_URL}}library/tests/results/)** - Detailed test data

### 🐳 Docker Container

Your PR has a dedicated Docker container published to GitHub Container Registry:

**Container Information:**
- **Package**: [GitHub Container Registry]({{PACKAGE_URL}})
- **Image Tag**: `ghcr.io/{{IMAGE_NAME_LOWER}}:pr-{{PR_NUMBER}}-latest`
- **Container Name**: `{{CONTAINER_NAME}}`
- **Port**: {{DYNAMIC_PORT}} (formula: 8080 + PR# % 100)

**Quick Start:**
```bash
# Pull the latest PR container
docker pull ghcr.io/{{IMAGE_NAME_LOWER}}:pr-{{PR_NUMBER}}-latest

# Run the container
docker run -d \
  --name {{CONTAINER_NAME}} \
  -p {{DYNAMIC_PORT}}:8080 \
  -e PR_NUMBER={{PR_NUMBER}} \
  -e DEPLOYMENT_ENVIRONMENT=preview \
  ghcr.io/{{IMAGE_NAME_LOWER}}:pr-{{PR_NUMBER}}-latest

# View logs
docker logs {{CONTAINER_NAME}}

# Access the container
docker exec -it {{CONTAINER_NAME}} pwsh
```

### 📈 What's Included
- ✅ Test execution results
- 📊 Code coverage metrics
- 🔍 Quality analysis
- 📋 Project status
- 🐳 Docker container ready to run

---
*🤖 Auto-generated by publish-test-reports workflow*
