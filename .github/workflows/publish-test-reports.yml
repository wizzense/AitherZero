---
name: 📊 Publish Test Reports & Dashboard to GitHub Pages

# Publish comprehensive test reports, dashboards, and PR-specific status to GitHub Pages
# Integrates with test-execution.yml and generates navigable reports for each PR
on:
  workflow_run:
    workflows: ["🧪 Test Execution (Complete Suite)", "✅ PR Validation (v2 - CLI)", "🔍 Quality Validation (v2 - CLI)", "🚀 PR Complete Workflow"]
    types: [completed]
  push:
    branches: [main, dev, develop, dev-staging, ring-0, ring-0-integrations, ring-1, ring-1-integrations, ring-2]
    paths:
      - 'library/reports/**'
      - 'library/tests/results/**'
      - 'library/tests/coverage/**'
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:
    inputs:
      force_publish:
        description: 'Force publish even if no changes detected'
        type: boolean
        default: true
      pr_number:
        description: 'PR number for PR-specific publishing (leave empty for main)'
        type: string
        required: false

permissions:
  contents: write
  pages: write
  id-token: write
  actions: read

concurrency:
  group: "pages-publish"
  cancel-in-progress: false

jobs:
  collect-reports:
    name: 📥 Collect Test Reports and Generate Dashboard
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref || github.event.repository.default_branch }}
          fetch-depth: 0

      - name: 🔧 Bootstrap Environment
        shell: pwsh
        run: |
          Write-Host "🚀 Bootstrapping AitherZero environment..." -ForegroundColor Cyan
          ./bootstrap.ps1 -Mode New -InstallProfile Minimal

      - name: 📊 Download Test Artifacts from test-execution.yml
        if: github.event.workflow_run
        uses: dawidd6/action-download-artifact@v6
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          run_id: ${{ github.event.workflow_run.id }}
          path: ./downloaded-artifacts
          workflow: test-execution.yml
        continue-on-error: true

      - name: 📦 Organize Test Results
        shell: pwsh
        run: |
          Write-Host "📦 Organizing test results from new test structure..." -ForegroundColor Cyan
          
          # Ensure directories exist
          New-Item -ItemType Directory -Path "library/tests/results" -Force | Out-Null
          New-Item -ItemType Directory -Path "library/tests/coverage" -Force | Out-Null
          New-Item -ItemType Directory -Path "library/reports" -Force | Out-Null
          
          # Copy downloaded artifacts to test results
          if (Test-Path "./downloaded-artifacts") {
            Write-Host "Copying test artifacts from test-execution.yml..." -ForegroundColor Yellow
            
            Get-ChildItem "./downloaded-artifacts" -Recurse -Filter "*.xml" | ForEach-Object {
              $destPath = Join-Path "library/tests/results" $_.Name
              Copy-Item $_.FullName -Destination $destPath -Force
              Write-Host "  ✓ Test Result: $($_.Name)" -ForegroundColor Green
            }
            
            Get-ChildItem "./downloaded-artifacts" -Recurse -Filter "*.json" | ForEach-Object {
              $destPath = Join-Path "library/tests/results" $_.Name
              Copy-Item $_.FullName -Destination $destPath -Force
              Write-Host "  ✓ Test Data: $($_.Name)" -ForegroundColor Green
            }
            
            # Copy coverage data
            Get-ChildItem "./downloaded-artifacts" -Recurse -Include "coverage*" | ForEach-Object {
              if ($_.PSIsContainer) {
                Copy-Item $_.FullName -Destination "library/tests/coverage" -Recurse -Force
              } else {
                Copy-Item $_.FullName -Destination "library/tests/coverage" -Force
              }
              Write-Host "  ✓ Coverage: $($_.Name)" -ForegroundColor Green
            }
          }
          
          Write-Host "✅ Test results organized" -ForegroundColor Green

      - name: 🎯 Run Comprehensive Tests for Dashboard
        shell: pwsh
        run: |
          Write-Host "🧪 Running tests to generate fresh metrics..." -ForegroundColor Cyan
          
          # Run quick validation to get current metrics
          if (Test-Path "./library/automation-scripts/0404_Run-PSScriptAnalyzer.ps1") {
            Write-Host "Running PSScriptAnalyzer for quality metrics..." -ForegroundColor Yellow
            ./library/automation-scripts/0404_Run-PSScriptAnalyzer.ps1 -Fast -ErrorAction Continue
          }
          
          # Run unit tests for test metrics
          if (Test-Path "./library/automation-scripts/0402_Run-UnitTests.ps1") {
            Write-Host "Running unit tests for coverage metrics..." -ForegroundColor Yellow
            ./library/automation-scripts/0402_Run-UnitTests.ps1 -NoCoverage -ErrorAction Continue
          }
          
          Write-Host "✅ Test execution completed" -ForegroundColor Green

      - name: 📊 Generate Comprehensive Dashboard
        shell: pwsh
        run: |
          Write-Host "📊 Generating comprehensive project dashboard..." -ForegroundColor Cyan
          
          # Generate main dashboard with all metrics
          ./library/automation-scripts/0512_Generate-Dashboard.ps1 -Format All
          
          # Generate project report
          if (Test-Path "./library/automation-scripts/0510_Generate-ProjectReport.ps1") {
            ./library/automation-scripts/0510_Generate-ProjectReport.ps1 -ErrorAction Continue
          }
          
          Write-Host "✅ Dashboard generated successfully" -ForegroundColor Green

      - name: 🏷️ Generate PR-Specific Dashboard with Container Info
        if: github.event.pull_request
        shell: pwsh
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BRANCH: ${{ github.event.pull_request.head.ref }}
          PR_BASE: ${{ github.event.pull_request.base.ref }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_SERVER_URL: ${{ github.server_url }}
        run: |
          Write-Host "🏷️ Generating PR-specific dashboard for PR #$env:PR_NUMBER..." -ForegroundColor Cyan
          
          $prDir = "library/reports/pr-$env:PR_NUMBER"
          New-Item -ItemType Directory -Path $prDir -Force | Out-Null
          
          # Copy main dashboard to PR-specific location
          if (Test-Path "library/reports/dashboard.html") {
            Copy-Item "library/reports/dashboard.html" "$prDir/dashboard.html" -Force
            Copy-Item "library/reports/dashboard.json" "$prDir/dashboard.json" -Force -ErrorAction SilentlyContinue
          }
          
          # Calculate Docker container info
          $dynamicPort = 8080 + ([int]$env:PR_NUMBER % 100)
          $imageNameLower = "$env:GITHUB_REPOSITORY".ToLower()
          $packageUrl = "https://github.com/$env:GITHUB_REPOSITORY/pkgs/container/aitherzero"
          $containerName = "aitherzero-pr-$env:PR_NUMBER"
          
          # Create PR-specific index with Docker container information
          $prIndexContent = @"
# 🎯 PR #$env:PR_NUMBER Dashboard

**Title:** $env:PR_TITLE  
**Branch:** ``$env:PR_BRANCH`` → ``$env:PR_BASE``  
**Status:** ${{ github.event.pull_request.state }}  
**Updated:** $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss UTC')

## 📊 Quick Links

- [View Dashboard](dashboard.html) - Comprehensive metrics and status
- [Back to Main Dashboard](../dashboard.html)
- [All PRs](/prs/)

---

## 🐳 Docker Container

### Container Information
- **Package**: [GitHub Container Registry]($packageUrl)
- **Image Tag**: ``ghcr.io/$imageNameLower:pr-$env:PR_NUMBER-latest``
- **Container Name**: ``$containerName``
- **Port**: $dynamicPort (formula: 8080 + PR# % 100)

### 📥 Pull & Run Commands

````bash
# Pull the latest PR container
docker pull ghcr.io/$imageNameLower:pr-$env:PR_NUMBER-latest

# Run the container
docker run -d \
  --name $containerName \
  -p ${dynamicPort}:8080 \
  -e PR_NUMBER=$env:PR_NUMBER \
  -e DEPLOYMENT_ENVIRONMENT=preview \
  ghcr.io/$imageNameLower:pr-$env:PR_NUMBER-latest

# View logs
docker logs $containerName

# Access the container
docker exec -it $containerName pwsh
````

### 🔗 Container Links
- **Package Page**: [$packageUrl]($packageUrl)
- **All Tags**: View all available tags for this PR in the package registry
- **Pull Command**: See above for the latest tag

---

## 📈 Latest Test Results

"@
          
          # Add test result summary if available
          $testResults = Get-ChildItem "library/tests/results" -Filter "*.xml" -ErrorAction SilentlyContinue | Sort-Object LastWriteTime -Descending | Select-Object -First 1
          if ($testResults) {
            try {
              [xml]$testXml = Get-Content $testResults.FullName
              $total = [int]$testXml.'test-results'.total
              $failures = [int]$testXml.'test-results'.failures
              $errors = [int]$testXml.'test-results'.errors
              $passed = $total - $failures - $errors
              
              $prIndexContent += @"

### Test Summary
- ✅ **Passed:** $passed
- ❌ **Failed:** $($failures + $errors)
- 📊 **Total:** $total
- 🎯 **Success Rate:** $([math]::Round(($passed / $total) * 100, 1))%

"@
            } catch {
              Write-Host "⚠️ Could not parse test results: $_" -ForegroundColor Yellow
            }
          }
          
          # Add deployment status section
          $prIndexContent += @"

---

## 🚀 Deployment Status

Check the [GitHub Actions workflow runs]($env:GITHUB_SERVER_URL/$env:GITHUB_REPOSITORY/actions) for:
- ✅ **Container Build**: View build logs and container publishing status
- ✅ **Test Execution**: See all test results and coverage
- ✅ **Quality Validation**: Code quality and linting results

### How to Test This PR

1. **Pull the Docker image** (see commands above)
2. **Run the container** with the provided command
3. **Access the environment** at ``http://localhost:$dynamicPort``
4. **Review the dashboard** for metrics and test results

---

*🤖 Auto-generated dashboard for PR #$env:PR_NUMBER*
"@
          
          $prIndexContent | Out-File -FilePath "$prDir/index.md" -Encoding UTF8
          
          Write-Host "✅ PR #$env:PR_NUMBER dashboard created at $prDir" -ForegroundColor Green
          Write-Host "  - Container: $containerName" -ForegroundColor Cyan
          Write-Host "  - Port: $dynamicPort" -ForegroundColor Cyan
          Write-Host "  - Package: $packageUrl" -ForegroundColor Cyan

      - name: 📑 Create Main Navigation Index
        shell: pwsh
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_SERVER_URL: ${{ github.server_url }}
        run: |
          Write-Host "📑 Creating main navigation index..." -ForegroundColor Cyan
          
          $timestamp = Get-Date -Format 'yyyy-MM-dd HH:mm:ss UTC'
          
          # Get all PR dashboards
          $prDirs = Get-ChildItem "library/reports" -Directory -Filter "pr-*" -ErrorAction SilentlyContinue | Sort-Object Name -Descending
          
          # Get latest release info from GitHub API or local VERSION file
          $latestRelease = "Unknown"
          $releaseUrl = "$env:GITHUB_SERVER_URL/$env:GITHUB_REPOSITORY/releases/latest"
          if (Test-Path "./VERSION") {
            $latestRelease = Get-Content "./VERSION" -Raw -ErrorAction SilentlyContinue
            if ($latestRelease) {
              $latestRelease = $latestRelease.Trim()
            }
          }
          
          # Calculate Docker container info
          $imageNameLower = "$env:GITHUB_REPOSITORY".ToLower()
          $packageUrl = "https://github.com/$env:GITHUB_REPOSITORY/pkgs/container/aitherzero"
          
          $indexContent = @"
# 🎯 AitherZero Project Dashboard

**Last Updated:** $timestamp

## 🚀 Main Dashboard

📊 [**View Comprehensive Dashboard**](library/reports/dashboard.html) - Real-time project metrics, test results, and code quality

---

## 📦 Latest Release

- **Version:** $latestRelease
- **Release Page:** [$releaseUrl]($releaseUrl)
- **Packages:** [GitHub Releases]($env:GITHUB_SERVER_URL/$env:GITHUB_REPOSITORY/releases)

### 🐳 Docker Images

- **Latest Release:** ``ghcr.io/$imageNameLower:latest``
- **Version Tag:** ``ghcr.io/$imageNameLower:$latestRelease``
- **Package Registry:** [$packageUrl]($packageUrl)

### 📥 Quick Install

````powershell
# One-line install (PowerShell)
iwr -useb https://raw.githubusercontent.com/$env:GITHUB_REPOSITORY/main/bootstrap.ps1 | iex

# Docker
docker pull ghcr.io/$imageNameLower:latest
docker run -it --rm ghcr.io/$imageNameLower:latest
````

---

## 📋 Pull Request Dashboards

"@
          
          if ($prDirs.Count -gt 0) {
            foreach ($prDir in $prDirs) {
              $prNum = $prDir.Name -replace 'pr-', ''
              $prIndexPath = Join-Path $prDir.FullName "index.md"
              
              if (Test-Path $prIndexPath) {
                $prContent = Get-Content $prIndexPath -Raw
                if ($prContent -match 'Title:\*\* (.+)') {
                  $prTitle = $matches[1].Trim()
                } else {
                  $prTitle = "PR #$prNum"
                }
              } else {
                $prTitle = "PR #$prNum"
              }
              
              # Include Docker container info for each PR
              $prDynamicPort = 8080 + ([int]$prNum % 100)
              $indexContent += "- [PR #$prNum - $prTitle](library/reports/$($prDir.Name)/) 🐳 ``ghcr.io/$imageNameLower:pr-$prNum-latest`` (port: $prDynamicPort)`n"
            }
          } else {
            $indexContent += "*No open PRs with dashboards*`n"
          }
          
          $indexContent += @"

---

## 📚 Additional Resources

- [Test Reports](library/tests/results/) - Raw test execution data
- [Coverage Reports](library/tests/coverage/) - Code coverage analysis
- [Quality Metrics](library/reports/psscriptanalyzer-fast-results.json) - PSScriptAnalyzer results
- [Project Reports](library/reports/) - All generated reports

### 🐳 Container Registry

All Docker containers are published to GitHub Container Registry:
- **Main Package:** [$packageUrl]($packageUrl)
- **Latest Release:** ``ghcr.io/$imageNameLower:latest``
- **PR Containers:** ``ghcr.io/$imageNameLower:pr-{number}-latest``
- **Version Tags:** ``ghcr.io/$imageNameLower:{version}``

### 📦 Release Artifacts

Each release includes:
- ✅ **Platform Package** - AitherZero-v{version}.zip/tar.gz
- ✅ **MCP Server** - @aitherzero/mcp-server@{version}
- ✅ **Docker Images** - Multi-platform (amd64, arm64)
- ✅ **Build Info** - Complete build metadata

---

## 🎓 Dashboard Guide

### Health Scores
- 🟢 **90-100**: Excellent
- 🟡 **70-89**: Good  
- 🟠 **50-69**: Needs Work
- 🔴 **0-49**: Urgent

### Priority Actions
1. Fix Critical/Error items
2. Address failing tests
3. Review warnings
4. Plan new features

---

*Generated: $timestamp*
"@
          
          $indexContent | Out-File -FilePath "index.md" -Encoding UTF8
          
          Write-Host "✅ Navigation index created" -ForegroundColor Green
          Write-Host "  - PRs tracked: $($prDirs.Count)" -ForegroundColor Cyan
          Write-Host "  - Latest release: $latestRelease" -ForegroundColor Cyan
          Write-Host "  - Container registry: $packageUrl" -ForegroundColor Cyan

      - name: 💾 Commit Updated Reports
        if: github.event_name != 'pull_request'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Add reports and dashboards
          git add library/reports/dashboard.* library/reports/psscriptanalyzer-fast-results.json
          git add library/reports/pr-*/
          git add library/reports/README.md library/reports/index.md
          git add library/reports/metrics-history/*.json
          git add index.md
          
          # Commit if there are changes
          if ! git diff --staged --quiet; then
            git commit -m "chore: update dashboards and reports - $(date -u +"%Y-%m-%d %H:%M UTC") [skip ci]"
            git push origin HEAD:${{ github.ref_name }}
            echo "✅ Reports committed and pushed"
          else
            echo "ℹ️ No changes to reports"
          fi

      - name: 📤 Upload Combined Reports
        uses: actions/upload-artifact@v4
        with:
          name: combined-reports
          path: |
            library/reports/
            library/tests/results/
            library/tests/coverage/
            index.md
            _config.yml
          retention-days: 30

  publish-to-pages:
    name: 🚀 Publish to GitHub Pages
    runs-on: ubuntu-latest
    needs: collect-reports
    timeout-minutes: 10

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 📊 Download Combined Reports
        uses: actions/download-artifact@v4
        with:
          name: combined-reports
          path: ./

      - name: 🔧 Setup Pages
        uses: actions/configure-pages@v5

      - name: 🏗️ Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site

      - name: 📤 Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site

      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: 📋 Report Deployment
        shell: pwsh
        env:
          PAGE_URL: ${{ steps.deployment.outputs.page_url }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          Write-Host "✅ Successfully deployed to GitHub Pages!" -ForegroundColor Green
          Write-Host "🔗 Main URL: $env:PAGE_URL" -ForegroundColor Cyan
          Write-Host ""
          Write-Host "📊 Available Dashboards:" -ForegroundColor Yellow
          Write-Host "  - Main Dashboard: ${env:PAGE_URL}library/reports/dashboard.html"
          Write-Host "  - Navigation Index: ${env:PAGE_URL}index.html"
          
          if ($env:PR_NUMBER) {
            Write-Host "  - PR #$env:PR_NUMBER Dashboard: ${env:PAGE_URL}library/reports/pr-$env:PR_NUMBER/"
          }
          
          Write-Host ""
          Write-Host "📂 All Reports: ${env:PAGE_URL}library/reports/"
          Write-Host "🧪 Test Results: ${env:PAGE_URL}library/tests/results/"
          Write-Host "📈 Coverage: ${env:PAGE_URL}library/tests/coverage/"

      - name: 💬 Comment on PR with Dashboard Link
        if: github.event.pull_request
        uses: actions/github-script@v7
        env:
          PAGE_URL: ${{ steps.deployment.outputs.page_url }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPOSITORY: ${{ github.repository }}
        with:
          script: |
            const pageUrl = process.env.PAGE_URL;
            const prNumber = process.env.PR_NUMBER;
            const repository = process.env.REPOSITORY;
            
            // Calculate Docker container info
            const dynamicPort = 8080 + (parseInt(prNumber) % 100);
            const imageNameLower = repository.toLowerCase();
            const packageUrl = `https://github.com/${repository}/pkgs/container/aitherzero`;
            const containerName = `aitherzero-pr-${prNumber}`;
            
            const comment = `## 📊 Dashboard & Container Published

Your PR-specific dashboard and Docker container have been published!

### 🔗 Quick Links
- **[View PR Dashboard](${pageUrl}library/reports/pr-${prNumber}/)** - Your PR's current status
- **[Main Project Dashboard](${pageUrl}library/reports/dashboard.html)** - Overall project health
- **[All Test Results](${pageUrl}library/tests/results/)** - Detailed test data

### 🐳 Docker Container

Your PR has a dedicated Docker container published to GitHub Container Registry:

**Container Information:**
- **Package**: [GitHub Container Registry](${packageUrl})
- **Image Tag**: \`ghcr.io/${imageNameLower}:pr-${prNumber}-latest\`
- **Container Name**: \`${containerName}\`
- **Port**: ${dynamicPort} (formula: 8080 + PR# % 100)

**Quick Start:**
\`\`\`bash
# Pull the latest PR container
docker pull ghcr.io/${imageNameLower}:pr-${prNumber}-latest

# Run the container
docker run -d \\
  --name ${containerName} \\
  -p ${dynamicPort}:8080 \\
  -e PR_NUMBER=${prNumber} \\
  -e DEPLOYMENT_ENVIRONMENT=preview \\
  ghcr.io/${imageNameLower}:pr-${prNumber}-latest

# View logs
docker logs ${containerName}

# Access the container
docker exec -it ${containerName} pwsh
\`\`\`

### 📈 What's Included
- ✅ Test execution results
- 📊 Code coverage metrics
- 🔍 Quality analysis
- 📋 Project status
- 🐳 Docker container ready to run

---
*🤖 Auto-generated by publish-test-reports workflow*`;
            
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber
            });
            
            const existingComment = comments.find(c => 
              c.user.login === 'github-actions[bot]' && 
              c.body.includes('Dashboard & Container Published')
            );
            
            if (existingComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existingComment.id,
                body: comment
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: comment
              });
            }
