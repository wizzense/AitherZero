name: 🔍 Comprehensive PR Validation

on:
  pull_request:
    branches: [ main, dev, dev-staging ]
    types: [opened, synchronize, reopened]

env:
  PR_NUMBER: ${{ github.event.number }}
  GITHUB_BASE_REF: ${{ github.base_ref }}
  GITHUB_HEAD_REF: ${{ github.head_ref }}

jobs:
  comprehensive-validation:
    name: ✅ Run Comprehensive Validation
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🚀 Bootstrap Environment
        shell: pwsh
        run: |
          ./bootstrap.ps1 -Mode New -InstallProfile Minimal -NonInteractive

      - name: 🔍 Run Comprehensive Validation Playbook
        shell: pwsh
        run: |
          Import-Module ./AitherZero.psd1 -Force
          
          Write-Host "╔═══════════════════════════════════════════════╗" -ForegroundColor Cyan
          Write-Host "║     COMPREHENSIVE PR VALIDATION PLAYBOOK      ║" -ForegroundColor Cyan
          Write-Host "╚═══════════════════════════════════════════════╝" -ForegroundColor Cyan
          Write-Host ""
          
          # Run the comprehensive validation playbook
          Invoke-OrchestrationSequence -LoadPlaybook validate-pr-comprehensive
          
          if ($LASTEXITCODE -ne 0) {
              Write-Host ""
              Write-Host "❌ Validation playbook FAILED" -ForegroundColor Red
              exit 1
          }
          
          Write-Host ""
          Write-Host "✅ All validations PASSED" -ForegroundColor Green
